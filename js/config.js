/**
 * 全局配置对象
 * 这个对象包含了整个项目所有的配置选项
 * 把所有配置集中在这里方便统一管理和修改
 */
const CONFIG = {
    // 第一部分：基本信息
    // version用来记录当前程序的版本号，方便追踪不同版本
    version: '1.0.0',    // 当前是1.0.0版本
    
    // 第二部分：功能开关
    // 这里集中管理所有可以开启或关闭的功能
    options: {
        // 是否自动播放视频
        // true: 视频添加后自动开始播放
        // false: 需要用户手动点击播放按钮
        autoPlay: false,        
        
        // 视频加载时是否静音
        // true: 视频第一次加载时是静音的
        // false: 视频加载时保持原有音量
        muteOnLoad: true,      
        
        // 视频是否循环播放
        // true: 视频播放完后自动重新开始
        // false: 视频播放完后停止
        loopVideos: false,     
        
        // 是否显示视频控制栏
        // true: 显示播放、暂停、音量等控制按钮
        // false: 隐藏控制栏
        showControls: true,    
    },
    
    // 第三部分：主题设置
    // 定义了明亮和暗黑两种主题的颜色方案
    theme: {
        // 明亮主题的颜色配置
        light: {
            primary: '#2c3e50',    // 主要颜色，用于标题、按钮等重要元素
            secondary: '#34495e',   // 次要颜色，用于边框、背景等次要元素
            // 这里可以添加更多颜色配置...
        },
        
        // 暗黑主题的颜色配置
        dark: {
            primary: '#1a1a1a',    // 深色背景下的主要颜色
            secondary: '#2d2d2d',   // 深色背景下的次要颜色
            // 这里可以添加更多颜色配置...
        }
    },
    
    // 第四部分：布局设置
    // 设置默认的网格布局和视频比例
    defaultLayout: '2x2',           // 默认使用2x2网格（2行2列，共4个格子）
    defaultAspectRatio: '16:9',     // 默认使用16:9的视频比例（适合横屏视频）
    
    // 第五部分：布局配置详情
    // 定义了不同布局模式下的具体参数
    layouts: {
        // 2x2布局的详细配置
        '2x2': {
            // 横屏模式下的布局参数
            landscape: { 
                columns: 2,     // 横向排列2个视频
                rows: 2,        // 纵向排列2个视频
                cells: 4        // 总共可以放4个视频
            },    
            
            // 竖屏模式下的布局参数
            portrait: { 
                columns: 3,     // 横向排列3个视频
                rows: 1,        // 纵向排列1个视频
                cells: 3        // 总共可以放3个视频
            }      
        },
        
        // 3x3布局的详细配置
        '3x3': {
            // 横屏模式下的布局参数
            landscape: { 
                columns: 3,     // 横向排列3个视频
                rows: 3,        // 纵向排列3个视频
                cells: 9        // 总共可以放9个视频
            },    
            
            // 竖屏模式下的布局参数
            portrait: { 
                columns: 6,     // 横向排列6个视频
                rows: 2,        // 纵向排列2个视频
                cells: 12       // 总共可以放12个视频
            }     
        }
    },

    // 第六部分：视频比例配置
    // 定义不同视频比例对应的显示模式
    aspectRatios: {
        '16:9': 'landscape',    // 16:9的视频使用横屏模式显示
        '9:16': 'portrait'      // 9:16的视频使用竖屏模式显示
    },

    // 第七部分：支持的视频格式
    // 列出所有程序支持播放的视频格式
    supportedFormats: [
        'video/mp4',    // 支持MP4格式（最常用的格式）
        'video/webm',   // 支持WebM格式（网页推荐使用的格式）
        'video/ogg'     // 支持Ogg格式（开源格式）
    ]
}; 